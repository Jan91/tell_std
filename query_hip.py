#! /usr/bin/python

from astroquery.simbad import Simbad
from bs4 import BeautifulSoup
from urllib import urlopen



def qu(obj):

	obj_str = obj
	
	#result_table = Simbad.query_object(obj_str)
	#print(result_table)
	
	#Simbad.list_votable_fields()
	
	customSimbad = Simbad()
	customSimbad.add_votable_fields('main_id', 'coordinates', 'propermotions', 'flux(V)')
	#customSimbad.get_votable_fields()
	
	
	obj = customSimbad.query_object(obj_str)
	#print(obj.keys())
	
	ra, dec, pm_ra, pm_dec = obj['RA'][0].split(), obj['DEC'][0].split(), obj['PMRA'], obj['PMDEC']
	flux_v = obj['FLUX_V']
	
	ra_str = str(ra[0])+':'+str(ra[1])+':'+str(ra[2])
	dec_str = str(dec[0])+':'+str(dec[1])+':'+str(dec[2])
	
	
	print str(ra_str) + ", " + str(dec_str) + ", " +  str(pm_ra[0]) + ", " +  str(pm_dec[0]) + ", " + str(flux_v[0])


qu('Hip047022')

qu('Hip064722')







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































#html = urlopen(url_str).read()
#soup = BeautifulSoup(html)

#divList = soup.find_all('tt', title="quality flag (A->E)")

#print divList


#obj = Simbad.query_object(obj_str)

#print result_table.keys()
#obj.pprint(show_unit=True)

#ra, dec = obj['RA'][0].split(), obj['DEC'][0].split()
#print str(ra[0])+':'+str(ra[1])+':'+str(ra[2]), str(dec[0])+':'+str(dec[1])+':'+str(dec[2])


